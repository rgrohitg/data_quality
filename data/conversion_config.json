{
  "default_checks": [
    { "check": "row_count > 0" }
  ],
  "column_checks": [
    { "check": "duplicate_count", "param_template": "{col}" }
  ],
  "schema_checks": {
    "default_schema": [
      {
        "name": "Confirm that required columns are present",
        "required_columns": [],
        "warn": {
          "when forbidden column present": [],
          "when wrong column type": {}
        },
        "fail": {
          "when required column missing": [],
          "when forbidden column present": [],
          "when wrong column type": []
        }
      }
    ],
    "optional": []
  }
}
